# Changelog

## 2.0.0 - 2026-07-31

- 将单文件脚本整理为可安装的 `src` 布局项目，同时保留 `/data/huangfy/omnitop.py` 兼容入口。
- 新增稳定 JSON/JSONL 快照、schema 版本、版本输出和运行时诊断。
- 修复非 TTY 启动永久挂起的问题。
- 把固定 20 FPS 重绘改为事件驱动重绘。
- 使用单调时钟计算速率，并用进程启动 token 隔离 PID 复用。
- 为进程信号操作增加 PID 1、自身进程和 PID 复用保护。
- 修复 compute/graphics GPU 进程显存重复累加。
- 新增 NVML 自动重试、单设备错误降级和 GPU 占用语义说明。
- 摘要视图延迟采集 PCIe、时钟、编码器、解码器和逐进程 I/O，降低常态开销。
- 新增用户/GPU-only/无进程模式、采集超时告警、窄终端布局和控制字符清洗。
- 新增自动化测试、静态检查配置和本地虚拟环境工作流。
